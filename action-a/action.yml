name: "Hello Actions"
description: "Greet someone"
author: "corey44"

inputs:
    MY_NAME:
        description: "Who to greet"
        required: true
        default: "World"
        
runs:
    using: "docker"
    image: "Dockerfile"
    
branding:
    icon: "mic"
    color: "green"
